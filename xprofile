#!/usr/bin/fish

### Housekeeping
# Load new fonts.
#xset +fp /usr/share/fonts/local
#xset fp rehash

# Merge xresources defaults.
$HOME/bin/xrr

# 🚫 caps lock.
xmodmap .capscontrol
#xmodmap .modswap
nitrogen --restore &
###

### Daemons
sxhkd &
synclient PalmDetect=1
synclient MaxTapTime=0
synclient VertScrollDelta=-127 # natural scrolling
syndaemon -d -i 0.5 -KR
compton & # Compositing
start-pulseaudio-x11 & # Audio

