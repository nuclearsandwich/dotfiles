super + {1-9,0}
  bspc desktop -f {term, www, chat, dev, mail, bonus, 5000, eight, nine, root}

super {_,shift} + {h,j,k,l}
  bspc window -{f,s} {left, down, up, right}

super + {comma,period}
	bspc window -f {prev,next}

super + Tab
  bspc desktop -f last

super + b
	bspc window -s biggest
